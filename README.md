# HydraRankSenai

## Como rodar o projeto

1. Clone o repositório
2. Copie `.env.example` para `.env` 
3. `npm install`
4. `npm run dev`

## Supabase

O cliente já está configurado em `src/supabaseClient.js`